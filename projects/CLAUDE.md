# Project work — conventions (auto-loaded under projects/)

When creating or maintaining any `projects/<name>/` tree in this vault, follow the reusable multi-repo conventions:

@../knowledge/multi-repo-conventions.md

Those conventions are generic and reusable across every project (delo, alrai, xyzbytes, kronos). **Project-specific** instructions — a single project's build/test commands, repo quirks, or house style — do NOT belong here or anywhere in the vault. They live in that project's own **code repo on the machine**, as the repo's `CLAUDE.md` (shared) or a gitignored `CLAUDE.local.md` (personal-only), so they load when you work in the repo rather than in the vault.
