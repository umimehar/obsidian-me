{
  "name": "investments",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run src/build.ts",
    "analytics": "bun run src/analytics/build.ts",
    "fixtures": "bun run src/tools/make-fixtures.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src",
    "check": "biome check src && tsc --noEmit && bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2"
  }
}
