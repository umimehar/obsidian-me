// Client entry point, bundled by render.ts's bundleClient() and inlined into
// the rendered page. Filter, charts, and sections land in later tasks.
const root = document.getElementById("ledger-data");
if (root) {
  // parsed below in later tasks
  void root;
}
