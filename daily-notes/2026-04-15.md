Today, I installed obsidian first time with claude code, I like this structure. 
I also filled out the eticket for Punta Cana trip and sent the QR code pdfs in whats app. 


---

## Session Log — 2026-04-15

### What Was Done
- Installed and configured **Obsidian MCP server** (`mcp-obsidian`) for Claude Code
  - Added to `~/.mcp.json` with Local REST API plugin on port 27124
  - Enabled in `~/.claude/settings.json`
  - API key stored in `~/.zshrc` as `OBSIDIAN_API_KEY`
- Created full **vault folder structure**: `inbox/`, `wiki/` (concepts, entities, sources), `projects/`, `daily-notes/`, `references/`, `templates/`
- Wrote **[[CLAUDE]]** orientation document with conventions, session commands, and layered reading strategy
- Created **[[wiki/hot]]** (session context cache) and **[[wiki/index]]** (master wiki index)
- Created note templates: `default-note.md` and `daily-note.md`
- Wrote comprehensive **[[usage]]** guide covering all folders, subfolders, prompts, and workflows
- Registered `/resume` and `/save` as proper Claude Code skills
- Moved vault from `~/Documents/Obsidian Vault/` to **Google Drive** for cloud sync
- Verified MCP connection works from new location

### Decisions Made
- Chose `mcp-obsidian` (REST API) over `obsidian-claude-code-mcp` (WebSocket) since REST API plugin was already installed
- Adopted **Karpathy LLM Wiki pattern** with compounding knowledge workflow
- Single vault approach (not per-project) for cross-project knowledge discovery
- Two vaults on Google Drive: `me/` (personal) and `dev/` (development)

### Open Loops
- [ ] Start using the vault for actual knowledge capture
- [ ] Try the compounding knowledge loop: dump raw material -> synthesize into wiki pages
- [ ] Set up Obsidian plugins: Templater, Calendar, Dataview
- [ ] Consider adding Graphify for codebase knowledge graphs


---

## Session Log — 2026-04-15 (Evening)

### What Was Done
- Moved both vaults from Google Drive to `~/obsidian/` for Git compatibility
- Initialized Git repos and pushed to private GitHub repos:
  - `me/` → `git@github.com:umimehar/obsidian-me.git`
  - `dev/` → `git@github.com:umimehar/obsidian-dev.git`
- Set up **dev vault** with project-centric structure: per-project logs, bugs, features, spikes, ADRs, knowledge base, standup, retros
- Created 5 dev templates: project-log, bug-report, feature-spec, decision-record, standup
- Updated `/resume` skill to `git pull --rebase` before reading context
- Updated `/save` skill to `git add -A && commit && push` after saving notes
- Added skill reference copies to both vaults under `skills/` for other machine setup
- Installed **Obsidian Git plugin** in dev vault for auto-sync (5 min interval)

### Decisions Made
- Moved off Google Drive to avoid Git + cloud sync conflicts
- Git for both vaults (not just dev) — full version history and cross-device sync
- Vault detection via root folder presence: `wiki/` = me, `knowledge/` = dev
- Obsidian Git plugin handles auto-sync when editing directly; `/save` and `/resume` handle Claude Code sessions

### Open Loops
- [ ] Install Obsidian Git plugin in **me** vault (only installed in dev so far)
- [ ] Configure Obsidian Git settings: 5 min auto-backup, pull on startup, push on backup
- [ ] Install recommended plugins: Templater, Calendar, Dataview
- [ ] Start actual knowledge capture — try the compounding workflow
- [ ] Consider Graphify for codebase knowledge graphs
